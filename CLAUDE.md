# K-SAJU Vietnam — 프로젝트 가이드

단일 파일(`index.html`)로 동작하는 모바일 웹 운세 서비스입니다. 아래 원칙은 v6.5(character-first)부터 적용됩니다.

## 제품 원칙

- **character-first**: Daily 화면의 메인 경험은 캐릭터와의 대화(질문 → 선택 → 카드 → 결과)다. 정적 카드 배열, 큰 스코어 헤더 같은 요소를 메인 화면에 상시 노출하지 않는다. 점수/행운 정보는 대화가 끝난 뒤 "오늘 요약"에서만 보여준다.
- **mobile-first**: 360~412px 폭(360x800, 390x844, 412x915)을 기준 뷰포트로 설계하고 검증한다. 데스크톱은 부차적 고려사항이다.
- **한 화면 긴 글 금지**: 한 번에 노출하는 텍스트는 짧게 유지한다. 설명은 대화체로 여러 번 나눠서 보여주고, 캐릭터의 speech bubble 한 번에 긴 문단을 넣지 않는다.
- **카드보다 대화가 중심**: 카드(운세 결과)는 대화 흐름상 필요한 시점에만 등장시킨다. 카드 전체를 항상 펼쳐 보여주는 UI(5장 fan, 전체보기 등)는 만들지 않는다.
- **Production 직접 실험 금지**: `main` 브랜치에서 직접 실험적인 변경을 커밋/배포하지 않는다.
- **feature branch 우선**: 모든 변경 작업은 feature 브랜치(`vX.Y-설명` 형식)에서 시작한다.
- **테스트 후 반영**: 실제 브라우저(Playwright 등)로 핵심 플로우를 QA하고, console/page error가 0인 것을 확인한 뒤에만 병합/반영한다.

## 구조 메모

- `FLOWS` / `FLOW_POOLS`: 캐릭터 대화 엔진(질문→선택→카드→결과→후속질문→종료). 새 주제를 추가할 때는 이 구조를 재사용한다.
- `dailyPools`: 오늘의 운세 콘텐츠 풀. 대화 결과나 "오늘 요약"에 흡수해서 사용하며, 5장을 한 번에 펼쳐 보여주는 용도로 되돌리지 않는다.
- 캐릭터 이미지는 `#flowGuide`(`.charLayer` 안)에 위치하며, 현재는 단일 이미지 fallback이다. 추후 layered/Rive/Lottie 애셋으로 교체할 수 있도록 `.charLayer`를 별도 레이어로 분리해 두었다.
- 캐릭터 상태: `idle`, `listening`, `thinking`, `talking`, `happy`, `surprise`, `serious` (`setCharacterState()` / `.charState-*` CSS).
